#include<stdio.h>

void main()
{
    // int a; b = a; //Invalid

    int v = 43^4;   // valid but return 47, not return power
    printf("%d", v);

    // char dt = "21 dec";     // invalid bcs dt is taken only single character.
}